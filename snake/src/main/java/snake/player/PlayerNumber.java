package snake.player;

/**
 * An enum that ensures we only need to handle either one or two players. This supports having
 * preset configurations for each player, e.g. sprite colors, controls, and a HUD layout
 * optimized for one or two players.
 *
 * @author Tessa Power
 */
public enum PlayerNumber {
    PLAYER_ONE, PLAYER_TWO
}
